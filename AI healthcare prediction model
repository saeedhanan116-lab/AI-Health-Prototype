# AI Health Prototype
# Predicting hospital patient visits using simple pattern analysis

# Sample data (patient visits over 7 days)
days = [1, 2, 3, 4, 5, 6, 7]
patient_visits = [50, 65, 80, 75, 90, 100, 120]

# Calculate average increase
total_increase = 0
for i in range(1, len(patient_visits)):
    increase = patient_visits[i] - patient_visits[i-1]
    total_increase += increase

average_increase = total_increase / (len(patient_visits) - 1)

print("Average daily increase:", average_increase)

# Predict next 3 days
last_day = days[-1]
last_visits = patient_visits[-1]

print("\nPredicted patient visits:")
for i in range(1, 4):
    predicted = last_visits + average_increase * i
    print("Day", last_day + i, ":", int(predicted), "visits")
